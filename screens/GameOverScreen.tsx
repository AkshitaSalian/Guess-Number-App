import * as React from "react";
import { View } from "react-native";

interface IGameOverScreenProps {}

const GameOverScreen: React.FunctionComponent<IGameOverScreenProps> = (
  props
) => {
  return <View></View>;
};

export default GameOverScreen;
